# Style profile (per-tenant, learned over time)

The `draft-reply` skill reads this file to match the user's writing voice. It starts as a
generic default (matching `_shared/style-guide.md`) and should be refined manually or — in a
later phase — automatically, from diffs between agent-drafted and human-approved-and-edited
replies.

- Tone: professional, warm, concise (default — replace once real examples are available).
- Typical sign-off: (none observed yet).
- Typical greeting: (none observed yet).
- Preferred reply length: short, matching the incoming message's length.

## Notes

(Add short, specific observations here as they're confirmed — e.g. "always signs off with
first name only, no title" — rather than broad generalizations.)
