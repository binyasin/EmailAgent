# VIP list (per-tenant)

Email addresses (or domains) that should always be triaged as `AI/Urgent`, read by the `triage`
skill's step 7. One entry per line. Empty by default — populate via the dashboard's Skill
Settings screen (Phase 3) or by editing this file directly in the meantime.

<!-- example:
ceo@example-client.com
@board-member-domain.com
-->
