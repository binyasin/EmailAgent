# VIP list (per-tenant)

GENERATED by control-plane/app/services/config_renderer.py from this org's VIP rules — edits
made directly to this file will be overwritten on the next config render. Manage these via the
dashboard's Skill Settings screen instead.

{% if rules -%}
{% for rule in rules -%}
{{ rule }}
{% endfor -%}
{%- else -%}
<!-- no VIP rules configured yet -->
{%- endif %}
